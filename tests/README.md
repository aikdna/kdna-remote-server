# Current and historical tests

`npm test` selects current-read.test.mjs and check-current-surface.mjs explicitly. The older .test.js files and runtime-candidates/runtime-tools fixtures document the pre-public-Read product; they are retained history and are not the current test entry or evidence of current compatibility. No historical fixture is packed into the current seven-member public artifact.
