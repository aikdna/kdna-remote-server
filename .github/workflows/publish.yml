name: Candidate publication gate
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'REMOTE_CANDIDATE_RELEASE_NOT_AUTHORIZED: current release review is required.'
          exit 1
